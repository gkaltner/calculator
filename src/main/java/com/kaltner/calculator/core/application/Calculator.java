package com.kaltner.calculator.core.application;

public interface Calculator {
    double add(int operator1, int operator2);

    double subtract(int operator1, int operator2);

    double multiply(int operator1, int operator2);

    double divide(int operator1, int operator2);

}
